# VeilKnit Mailer on Windows

Build everything with `build_daemon_and_mailer.bat`, or build the daemon and `Source\VeilKnitMailer_cpp\VeilKnitMailer.sln` separately.

The daemon must be running and logged in before Mailer can connect. On first launch, approve the request number from the daemon's Applications page, then click **Check Mailer approval**.
