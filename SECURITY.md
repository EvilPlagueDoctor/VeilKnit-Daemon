# Security policy

Do not post private keys, application credentials, user databases, mailbox state, signing keystores, or unredacted logs in public issues. Report security-sensitive findings privately to the repository owner until a dedicated disclosure address is established.

Public binaries should be built with the privacy-hardened release scripts and scanned as described in `PRIVACY_BUILD.md`.
