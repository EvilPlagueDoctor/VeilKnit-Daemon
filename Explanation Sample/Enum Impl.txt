BackgroundCommand (enum) 
    V
"What messages can be sent?"

Background (struct)
    V
"What state does this module own?" - this includes the commands (enum)

impl Background
    V
"What public functions can other code call?" - the actual functions.