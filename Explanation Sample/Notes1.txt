dht_store should have

a struct  that holds all the information about our DHT's (when it gets loaded up, it'll get flagged as 'ours' and obviusly when we create it we know it's ours)

contains things like:
Record key.
sub keypairs (and their respective locations inside the DHT)
the information needed to let us know if we've opened the record or not.



o6U0W7aiiS1mn64kHqqnSyikdnZrVl7R02WBGAPs_ds:3zpsCziA8R24iTpqHR0EwnimLV-QCmhawhlDfg5TeJI

writing 250 values took like, 20 seconds concurrently.


(after setting up a node)

starts a new background (tokio::spawn) process.

this process basically waits for a command (function call?), and then executes that command.

Lets start off with 2 things.

one that takes 2 numbers, and adds them together, returning the result.

one that takes 1 number, and goes through a loop.

takes the one number and adds 1,
the the one number and adds 2,
etc, up until it reaches the number it was given. (so if given a 3, it will loop through 3+1, 3+2, 3+3, then end.)

---------------------------------

have one of the commands create a DHT or load up a DHT + a temporarily save to disk feature.

-creates a DHT, and saves the info to the HDD.
	-should store all the information locally to a struct.

in the main program, have it check to see if a file exists, if it does, load it up, and pass it in to the 'load DHT'  function.